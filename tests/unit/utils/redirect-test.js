import redirect from 'ember-paper-capture/utils/redirect';
import { module, test } from 'qunit';

module('Unit | Utility | redirect');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = redirect();
  assert.ok(result);
});
